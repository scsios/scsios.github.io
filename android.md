---
layout: default
category: tools
docid: index
title: Android で読み上げ
author: gusni.kantu@gmail.com
ccbysa: false
mit: true
date: 2026-09-24T07:43:00Z
modi: 2026-09-24T07:43:00Z
---
# Android で TalkBack などを使う
{:.no_toc}

- Android で読み上げ機能を使う場合の便利な使い方や機種特有の機能などを説明します。

## 目次
{:.no_toc}

* TOC
{:toc}

## 1. Pixel で カメラに写る範囲の説明を聞く

Pixel 特有の機能。 TalkBack をオンにした状態でカメラを起動し、指2本で下にスワイプすると、カメラに写る範囲の様子が音声で説明される。この機能を使うにはインターネット接続が必要。

## 2. MIVE ケースマで TalkBack をキー操作

MIVE ケースマは Android スマホの一種だが、昔の携帯電話のような物理キーボードが付いている。 TalkBack や VoiceOver でのジェスチャー（画面上の指操作）が不得手な人は、操作性を期待してこの機種を入手することがある。

しかし、 TalkBack をオンにしても、矢印キーや Enter キーが TalkBack ジェスチャーに対応した挙動にならないので使いにくい。

そこで、キーマップを変更して直感的な TalkBack 操作ができるようにする方法を以下に示す。

1.  Google Play Store から [Key Mapper & Floating Buttons](https://play.google.com/store/apps/details?id=io.github.sds100.keymapper)をケースマにインストールする。
2. Key Mapper & Floating Buttons アプリを開き、自分で好みのキーマップを設定するか、以下のキーマップデータをダウンロードしてインポートする。  
    インポートすると talkback グループや input グループが作られる。各グループを開くと、各キーマップの中にメッセージが出ているので、それぞれを了解したり許可したりする。そのためには Android の「開発者向けオプション」を有効に（Androidの設定＞デバイス情報＞ビルド番号を連打）しておく必要がある。メッセージがなくなればOK。  
    Android の「設定＞ユーザー補助＞TalkBack＞設定＞キーボードショートカット」で、デフォルトキーマップを選択、修飾キーは Alt を選択しておく。また、「読み上げコントロールを上に調整する」を Alt+上矢印（お気に入りボタンを押しながらリング状ボタンの上部を押す）、「読み上げコントロールを下に調整する」を Alt+下矢印（お気に入りボタンを押しながらリング状ボタンの下部を押す）に変更しておくと実用的になる。
   1. キーマップデータその1： [TalkBack ジェスチャーの代わりにボタンを押す](./files/keymaps_talkback.json)
      - SOSボタン（F4キー）短押し：指2本1回タップに相当する。  
      1回短く押すと、読み上げの一時停止・再開を切り替える。  
       素早く2回押すと指2本ダブルタップ、つまりマジックタップに相当し、メディア音声の一時停止・再開や、電話の受話・終話になる。（素早く5回押すと、設定によっては緊急SOSになるので注意）
      - お気に入りボタン（F3キー）長押し： Alt キーを押し続けることに相当する。長押しすると「TalkBack 修飾キー」と読み上げられる。  
       押している間だけ、TalkBackのデフォルトキーマップのキーボード・ショートカットが使える。
      - お気に入りボタンを押したまま、リング状のボタンの右部を短く押す：右スワイプに相当。（お気に入りボタンを押さないと単なる右矢印なので、右端に来ると次の行に行かずに止まってしまう）
      - お気に入りボタンを押したまま、リング状のボタンの左部を短く押す：左スワイプに相当。
      - お気に入りボタンを押したまま、リング状のボタンの中央を短く押す：ダブルタップに相当。（お気に入りボタンを押さないと単なる Enter なので、TalkBack のフォーカスと一致しない位置でアクティベートされる場合がある）
      - お気に入りボタン（F3キー）短押し： 指3本上スワイプに相当する。短押しする度に、前の読み上げコントロールに変わる。（TalkBack の設定で「読み上げコントロールを上／下に調整する」という項目を「Alt+上／下矢印」に変更してあれば、この操作の後に、お気に入りボタンを再度押したままリング状ボタンの上部か下部を押すと、その読み上げコントロールを調整できる。）
      - お気に入りボタン（F3キー）素早く2回押し： 指3本タップに相当する。TalkBack メニューが出る。
      - カメラボタン長押し： 左Ctrlキーに相当する。例えば、お気に入りボタンを押しながらカメラボタンを押しながらリング状ボタンの左／右部を押すと一番上／一番下にフォーカスが飛ぶようにしてみたのだが、3つのボタンを同時に押すのはいまいち使いにくいので、不要ならこのキーマップをオフにするか、別のTalkBackジェスチャーに割り当てても良いと思う。オフにするには、Trigger: Long press Camera の設定を開き、Enabled の次にある切り替えボタンをオフにし、最後の Done ボタンを押す。
   2. キーマップデータその2： [メッセージボタン長押しで iWnn IME と Google 音声入力を切り替える](./files/keymaps_im.json)  
    iWnn IME と Google 音声入力は、はじめからインストールされている。 Gboard はインストールされていないので指2本ダブルタップによる音声入力のオンオフができない。また、 Gboard をインストールしてもデフォルトキーボードの変更ができない。  
    iWnn IME の画面キーボード上には Google 音声入力への切り替えボタンがあるが、物理ボタンの割り当てが見つからなかったので、メッセージボタン長押しで入力方法を切り替えられるようにした。
3. 電源ボタン長押しで Gemini が出るようにするには、以下の2つを行う。
   1. Playストアで Google Go をアップデートする。  
    出荷時の状態では Gemini をブラウザ上でしか使えないが、 Google Go をアップデートすればシステム上で Gemini を使えるようになる。
   2. Android の「設定＞システム＞ジェスチャー＞電源ボタンを長押し＞デジタルアシスタント」を選択する。  
    出荷時の状態では電源ボタン長押しで電源ボタンメニューが出る。また、ホームボタン長押しで通知シェードが出る。いずれにしてもアシスタントが出ない。  
    この設定の場合、電源ボタンメニューを出すには電源ボタンと音量下ボタンを同時に長押しする。あるいは、電源ボタンを長押ししすぎると電源が切れる。

以上の設定でまあまあ快適に使えるようになるが、依然として機種特有の難点はある。

1. キー操作に対する反応が遅い。特に TalkBack オンの状態で文字入力を始めると、いちいち iWnn が起動したことが読み上げられて煩わしい。一旦キーを押したら、 TalkBack が読み終わるのを気長に待つと間違いが起きにくい。
2. デフォルトキーボードを iWnn 以外のものに変更できない。
3. システムの言語として、英語・韓国語・日本語以外の言語を選べない。
4. Gemini の返事の読み上げが途中で止まってしまうことがある。画面を触れば全部読み上げる。
5. Google Go による制限かもしれないが、電源ボタン長押しでの Gemini は文脈の引き継ぎができない。1回目の回答を踏まえて2回めに違う質問をしても、理解されない。


